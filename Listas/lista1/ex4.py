### **Exercício 4: Criando Lista com `range()`**

# Utilize a função `range()` para criar uma lista chamada `sequencia`
# contendo os números de 10 a 20 (inclusive). Imprima a lista.
sequencia = list(range(1, 21))

print(sequencia)
