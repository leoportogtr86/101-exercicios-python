# 12. **Contando Elementos**: Dada a tupla `(3, 4, 3, 2, 3, 1)`, utilize
# o método `count()` para contar quantas vezes o número 3 aparece na tupla.
from itertools import count

tupla = (3, 4, 3, 2, 3, 1)

print(tupla.count(3))
